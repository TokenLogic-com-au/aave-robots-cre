# ==========================================================================
# CRE WORKFLOW SETTINGS — Aave Depositor (one workflow per chain)
# ==========================================================================
aave-depositor-ethereum-production-settings:
  user-workflow:
    workflow-name: "aave-depositor-ethereum-production"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.ethereum.json"
    secrets-path: ""
